# Filter Manage — Domain Language

ICC / NVIDIA 配色方案管理。术语只描述业务概念，不涉及实现细节。

## Language

### 方案

**方案**:
一套可命名、可复用的显示器配色预设，包含颜色参数与展示用名称/图标。
_Avoid_: 配置、预设（口语可混用，文档与 UI 统一称「方案」）

**颜色参数**:
构成方案视觉效果的字段：亮度、对比度、伽马、数字振动、RGB 增益、ICC 配置。
_Avoid_: 设置、属性（过宽）

**RGB 增益**:
红 / 绿 / 蓝三通道各自的强度增益，用于偏色与白平衡感（对齐显示器 OSD「用户色温」下的 R/G/B，而非 NVIDIA 分通道亮度/对比度/伽马）。属于颜色参数，进入方案；加载方案、更新方案、另存为、托盘/快捷键/进程监听 apply 时一并生效。**内部统一标度**为每通道 -100 ~ +100、默认 0（无偏色）；界面展示由**调节方式**换算，不另存三套数。
_Avoid_: RGB 通道调节（易与 NVIDIA 全通道影调混淆）；色温（若仅三滑条、无一键冷暖预设，勿单称色温）

**调节方式**:
仅影响 RGB 增益滑条的**显示标度与默认刻度位置**，不改变底层颜色参数。三种：
- **NVIDIA**：显示 -100 ~ +100，默认 0（与内部一致）
- **AOC**：显示 0 ~ 100，默认 50（内部 = (显示 - 50) × 2）
- **卓伟**：显示 0 ~ 100，默认 100（内部 = 显示 - 100；从 100 往下拉减弱该色）
调节方式是界面偏好（非方案字段）；切换方式时保留内部值，只改刻度读法。
_Avoid_: 方案（易与可保存的配色方案混淆）；厂商色彩引擎（并非三套 API）

**基础参数**:
主界面「显示参数调整」内的二级分组标题；承载亮度、对比度、伽马、数字振动。

**显示色彩调整**:
主界面「显示参数调整」内、用于 RGB 增益的二级分组；置于基础参数之下，含调节方式选择器与红 / 绿 / 蓝三条滑条。
_Avoid_: 色彩管理（易与 ICC 混淆）

**当前方案**:
用户界面中标记为「正在应用」的那一个方案；更新操作以此为写回目标。
_Avoid_: 选中项、active config（对用户说「当前方案」）

**当前方案不可删除**:
仅禁止删除「正在应用」的那一个方案（删除入口禁用或拒绝）。其它方案可删，即使仍被快捷键或进程规则引用（引用完整性不在本能力范围内）。异常情况下存储中已无该方案时，「更新方案」应失败并清空当前方案，回到新建保存路径。
_Avoid_: 静默删除后清空（应先阻止删除）；按引用全局禁删（过宽）

**方案元数据**:
方案的名称与图标；由「编辑方案」或「另存为」流程维护，**不**由「更新方案」修改。

### 保存动作

**更新方案**:
把当前主界面上的颜色参数写回**当前方案**，不改名称、图标，也不打开命名对话框。成功后视为「界面与方案一致」：清除有改动、保持当前方案不变；不因更新再次向显示器 apply（参数通常已在调节时生效）。写盘后该方案的单一存储定义已变，托盘/快捷键/进程监听等**下次加载**即用新参数，不维护旧快照。
_Avoid_: 保存、覆盖保存、应用更改（易与「应用到显示器」混淆）

**另存为**:
以当前主界面颜色参数创建**新方案**，需命名（及可选图标）。名称不得与已有方案重复；冲突则拒绝并提示，不静默覆盖。
_Avoid_: 保存方案（在已有当前方案且有改动时，主按钮应是「更新方案」；「保存方案」留给无当前方案时的新建）

**保存方案**:
在没有当前方案时，把当前颜色参数存成新方案的动作（沿用现有命名弹窗）。无当前方案时右上角只显示此按钮（有改动才可点）；不展示「更新方案 / 另存为」双按钮。

### 改动状态

**有改动**:
主界面颜色参数相对「加载当前方案时（或重置基线时）的快照」发生了变化。
_Avoid_: dirty、未保存（对用户可用「有未保存的修改」等文案，领域上称「有改动」）

**切换方案与有改动**:
加载另一方案（含列表/托盘/快捷键/进程监听）时，未通过「更新方案」或「另存为」落盘的改动直接丢弃，不弹确认。

**重置默认（含 RGB）**:
「重置默认」将全部颜色参数恢复为中性默认（亮度/对比度/伽马/数字振动的既有默认，以及 RGB 增益三通道均为 0），并应用到当前显示器；ICC 是否一并恢复默认遵循既有重置逻辑，不因 RGB 单独例外。
_Avoid_: 重置时保留偏色（会让用户以为没重置干净）

**颜色参数实时生效**:
主界面调节颜色参数（含 RGB 增益）时，拖动即写入当前显示器输出，无需单独「应用」按钮；与「更新方案」落盘是两件不同的事。
_Avoid_: 应用更改（易与更新方案混淆）

**更新方案（按钮态）**:
存在当前方案时始终展示；仅在「有改动」时可点，无改动时禁用。有当前方案时与「另存为」主次并排（主实心 / 次描边），不使用分裂菜单。
**另存为（按钮态）**:
存在当前方案时始终展示且可点（允许以当前参数复制为新方案，不要求有改动）。

### 关于与反馈

**关于应用**:
展示应用身份信息（名称、版本、简介）与相关操作入口的弹窗；从设置中打开。
_Avoid_: 设置页本体（关于是独立弹窗）

**意见反馈**:
用户向产品方提交使用意见、问题或建议的动作；入口在关于弹窗，通过应用内表单完成，而非仅打开外链。
_Avoid_: 访问官网、检查更新（相邻但不同意图）

**意见反馈（分阶段）**:
第一阶段仅落地关于弹窗入口与关闭交互；完整表单与提交服务在服务端契约就绪后接入。
