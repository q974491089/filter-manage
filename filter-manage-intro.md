# Filter Manage — 你的屏幕，该由你说了算

> 一个只有几 MB 的 Windows 桌面工具，用 Rust 原生构建，专门管理 ICC 颜色配置文件和 NVIDIA 显卡颜色参数。没有广告，没有弹窗，没有多余的东西。调完即走，干净利落。

---

## 为什么做这个？

你一定经历过这样的"仪式"：开黑之前先手动切 ICC 配置文件，再打开 NVIDIA 控制面板拉亮度、对比度、伽马、数字振动，打完游戏想正常用电脑又得改回来……下次再玩？重新来一遍。

市面上不缺滤镜软件，但它们要么捆绑了一堆用不上的功能，要么用 Electron 打包成上百 MB 的"巨无霸"。

Filter Manage 只想做一件事——让你用最少的操作，把屏幕调到你最舒服的状态。

**几 MB 的体积，Rust 原生构建，退出后滤镜依然生效，不常驻不偷跑，零干扰。**

---

## 亮色 & 暗色，随心切换

Filter Manage 提供浅色和深色两套主题，还支持"跟随系统"模式。不管是白天码代码还是深夜肝游戏，眼睛都不会太累。

三档主题模式一键切换：浅色模式、深色模式、跟随系统。主题偏好会自动记住，下次打开还是你上次的选择。

<!-- 📷 配图位置：亮色模式界面截图 -->
![亮色模式](图片占位-亮色模式.png)

<!-- 📷 配图位置：暗色模式界面截图 -->
![暗色模式](图片占位-暗色模式.png)

---

## 核心功能一览

### ICC 配置文件管理

自动扫描系统 ICC 配置文件目录，列出所有可用的配置文件。点击即可切换，支持导入外部 `.icc` / `.icm` 文件。内置多个常用 ICC 配置文件，安装即用。

支持多显示器——如果你有多屏环境，可以针对每块屏幕独立切换 ICC 配置。

### NVIDIA 颜色精细调节

通过直观的滑块调节四项核心参数：

| 参数 | 范围 | 默认值 |
|------|------|--------|
| 亮度 | -125 ~ 125 | 0 |
| 对比度 | -82 ~ 82 | 0 |
| 伽马 | 0.1 ~ 3.0 | 1.0 |
| 数字振动 | 0 ~ 100 | 50 |

所有调节实时生效，配合内置参考图片可以直观看到效果变化。同样支持多显示器，顶部标签页切换即可针对不同屏幕分别调节。

### 方案预设，一键切换

调好参数后保存为一个方案（比如"FPS 游戏"、"看电影"、"日常办公"），下次一键加载。每个方案可以自定义图标，支持重命名和管理。

---

## 设置面板详解

点击界面右上角的齿轮图标进入设置面板，这里有四个标签页，覆盖了 Filter Manage 的全部进阶能力。

### 常规设置

管理应用的核心运行行为：

- **界面外观**：浅色模式 / 深色模式 / 跟随系统，三选一
- **关闭行为**：点击关闭按钮时，是最小化到系统托盘还是直接退出？首次会弹窗询问，之后按你的选择执行
- **开机自启**：随 Windows 自动启动，开机即就绪
- **通知偏好**：切换方案时是否弹出 Toast 提示

<!-- 📷 配图位置：常规设置面板截图 -->
![常规设置](图片占位-常规设置.png)

### 快捷键

全局快捷键绑定，让你不打开主窗口就能切换方案：

- **系统快捷键**：绑定"恢复默认设置"的快捷键，一键回到初始状态
- **方案快捷键**：为每个已保存的颜色方案录制专属快捷键，按下即刻生效

录制方式很直觉——点击输入框，按下你想用的组合键，搞定。如果快捷键冲突会即时提示，不用担心误操作。

<!-- 📷 配图位置：快捷键设置面板截图 -->
![快捷键](图片占位-快捷键.png)

### 显示适配

决定哪些方案出现在系统托盘的右键菜单中。最多选 5 个常用方案，右键托盘图标就能快速切换，不用打开主界面。

没有选中任何方案时，托盘菜单会自动展示前 5 个方案。灵活又省心。

<!-- 📷 配图位置：显示适配面板截图 -->
![显示适配](图片占位-显示适配.png)

### 进程监听

这是 Filter Manage 的"杀手级功能"——根据正在运行的进程自动切换配色方案。

使用方法很简单：

1. 点击"添加规则"，从当前运行的进程列表中选择（也可以手动输入进程名）
2. 为这条规则绑定一个配色方案
3. 开启"退出时恢复"选项（默认开启），当进程关闭后自动切回上一方案

规则按列表顺序匹配，第一个命中的生效。比如你可以设置：启动《三角洲行动》时自动切到 FPS 游戏方案，退出后恢复日常方案。全程无感，无需手动操作。

进程监听支持总开关控制，自动切换时可以选择是否弹出通知。

<!-- 📷 配图位置：进程监听面板截图 -->
![进程监听](图片占位-进程监听.png)

---

## 更多亮点

**系统托盘常驻**：最小化到托盘后，右键菜单快速切换方案，不占用任务栏空间。

**自动更新**：启动时自动检查新版本，更新内容弹窗一目了然，也可以选择"30 天内不再提醒"。支持手动检查更新。

**配置备份与恢复**：所有方案和应用设置保存在单个 `app.json` 文件中，原子写入 + 自动备份，文件损坏时自动恢复。方便整体备份或迁移到新电脑。

**多显示器支持**：顶部标签页切换不同显示器，每块屏幕独立调节 ICC 配置和 NVIDIA 参数，互不干扰。

**退出后滤镜依然生效**：Filter Manage 采用底层系统调用，设置完颜色参数后即使关闭软件，效果依然保留。不常驻后台，不占内存，不增加系统负担。

---

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | React 18 + TypeScript + Tailwind CSS |
| 后端 | Rust (Tauri 2.x) |
| 平台 | Windows 10/11 (64-bit) |
| 安装包 | NSIS，仅几 MB |

---

## 下载安装

| 渠道 | 说明 |
|------|------|
| [GitHub Releases](https://github.com/q974491089/filter-manage/releases) | 官方发布，含签名校验 |
| [夸克网盘](https://pan.quark.cn/s/b45313e695a4) | 国内高速下载 |
| [阿里云盘](https://www.alipan.com/s/JD7d7ZXwGtP) | 国内高速下载 |
| [蓝奏云](https://wwbrt.lanzouu.com/b01d704vbe) (密码: 5zm7) | 国内高速下载 |
| [百度网盘](https://pan.baidu.com/s/1fiyo5RhYixF-43tuQTrUjg) (密码: q227) | 国内高速下载 |

所有渠道的安装包内容一致，选速度快的就行。

---

## 系统要求

- Windows 10/11 (64-bit)
- NVIDIA 显卡（颜色调节功能需要）
- WebView2 Runtime（Windows 11 已内置，Windows 10 安装包会自动安装）

---

## 开源 & 反馈

Filter Manage 是一个开源项目，采用 MIT 许可证。

- GitHub: [github.com/q974491089/filter-manage](https://github.com/q974491089/filter-manage)
- 文档站: [filter-manage.pages.dev](https://filter-manage.pages.dev)
- 问题反馈: [GitHub Issues](https://github.com/q974491089/filter-manage/issues)

欢迎 Star、提 Issue、贡献代码，也欢迎加入 QQ 交流群反馈问题和分享使用心得。

---

**把选择权还给用户，把性能留给游戏。**
