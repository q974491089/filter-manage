# Changelog

## v0.3.5 — 2026-07-14

### 新功能

- 应用内公告：顶栏铃铛、未读红点、列表预览与详情弹窗、重要公告启动弹窗；随检查更新一并拉取；支持置顶与排序
- 单实例运行：重复启动时聚焦已有窗口

### 修复

- 进程监听：订阅前已在运行的进程退出后，能正确触发退出恢复（恢复默认配色方案）
- 明确「退出时恢复」语义为恢复默认方案（非上一手动方案）

### 改进

- 更新弹窗加宽，便于阅读较长更新说明

## v0.3.4 — 2026-07-04

### 新功能

- 自定义更新流程：多镜像下载 + 实时测速 + 慢速换源 + 可取消，替代 Tauri 内置 updater 直连 GitHub，解决国内用户直连 GitHub 超时问题
- 发版时 CI 自动推送版本清单（含 minisign 签名）到自建更新服务，客户端可获取服务端下发的镜像列表

### 修复

- 修复单点 DNS 故障导致检查更新失败：check_update 改为双域名竞速（tokio::select! 先到先得），任一 host 可达即用
- 修复 v0.3.3 更新清单 signature 为空导致客户端无法校验安装包完整性的问题（CI 推送 step 从 .sig 文件取签名）

### 改进

- 首页下载按钮样式与功能预览动画优化

## v0.3.3 — 2026-06-17

### 改进

- 图标系统迁移：从字体图标（Material Symbols）迁移到 React 组件图标（react-icons）
  - 解决字体加载依赖问题
  - 更好的 TypeScript 支持和 IDE 自动补全
  - 按需加载，减少打包体积
  - 统一图标 API，提升维护性

## v0.3.2 — 2026-06-15

### 修复

- 修复字体加载失败导致界面图标显示为英文的问题（Material Symbols 和 Inter 字体从 Google Fonts CDN 改为本地打包，解决国内网络环境下字体无法加载的问题）
- 修复监听规则弹窗保存按钮不可见的问题（进程选择器弹窗内容溢出导致底部按钮被裁剪）

## v0.3.1 — 2026-06-14

### 修复

- 修复全局设置中「添加规则」按钮不显示的问题（进程选择器弹窗内容溢出导致 footer 被裁剪）

## v0.3.0 — 2026-06-13

### 改进

- 设计系统全面优化：移除玻璃拟态效果，统一暗色调风格
- 字体系统统一：标题、正文、标签文字大小和行高规范化
- 滑块组件体验优化：间距调整、键盘步进行为改进、说明文字改为 info icon + tooltip
- Toggle 开关无障碍改进：增加 aria 属性支持
- 进程规则列表 UI 改进：优化卡片样式、圆角、间距、悬停效果
- 新增 TextSwitch 组件：用于"退出时恢复"等二态文字切换
- 进程选择器弹窗尺寸优化：从 400×500 调整为 500×600
- 进程规则空状态增加说明文字
- 配置管理器操作按钮可见性优化

### 文档

- 完善多 Agent 协作框架文档（`.agent/`、`.rules/`、`.skills/` 目录）
- 新增 CodeGraph 工具使用规则和优先级说明
- 新增进程监听 API 文档（`.docs/api/process_watcher.md`）
- 新增前端交接文档（`.docs/handoff/`）

## v0.2.9 — 2026-06-02

### 新功能

- 新增方案重命名功能（`rename_config`）
- 关闭行为改为三态下拉选择：最小化到托盘 / 直接关闭 / 未设置（首次弹窗询问）
- 托盘无可用方案时隐藏「快速方案」子菜单

### 改进

- 配置存储重构为单文件 `app.json`（颜色预设 + 应用设置合并），便于整体备份与未来云端同步
- 配置写入改为原子操作并保留备份（`app.json.bak`），文件损坏时自动恢复
- 自动兼容迁移旧版配置格式（多文件预设 / profiles.json / __settings__.json）
- 设置面板标题描述区域固定，关闭按钮不再随内容滚动
- 设置面板整体样式适配浅色/深色模式
- 点击设置项文字区域可直接切换开关控件
- 统一设置面板内卡片样式，提升视觉层次感
- 托盘方案切换后实时刷新系统托盘菜单
- "关于应用"页面复用 AboutModal 组件
- 快捷键录制时暂停全局快捷键，避免按键冲突
- 快捷键/托盘应用方案后前端 UI 自动同步（滑块/ICC/方案高亮）
- 方案列表显示"正在应用"标识

### 修复

- 修复鼠标拖动窗口时 ICC 列表意外刷新的问题（移除 focus 事件监听）
- 修复品牌名称 "Lumina" 相关文案统一为 "Filter Manage"
- 修复 Toast 提示被设置弹窗遮挡的问题（z-index 调整）
- 修复快捷键冲突提示显示 `__default__` 的问题（改为显示"恢复默认设置"）
- 修复关闭弹窗逻辑：使用后端 `close_prompted` 字段替代 localStorage，确保窗口正确隐藏到托盘

## v0.2.8 — 2026-05-25

### 修复

- 修复自动更新检查相关问题
- 新增手动检查更新功能（关于弹窗内可手动触发）

## v0.2.7 — 2026-05-25

### 改进

- 优化前台界面样式展示
- 更新文档站亮色/暗色模式对比截图

## v0.2.6 — 2026-05-24

### 新功能

- 集成 `tauri-plugin-opener`，支持调用系统默认浏览器打开外部链接（用于更新弹窗中的「查看 Release Notes」等场景）

### 改进

- 自动更新提示由顶部横幅(Banner) 升级为弹窗(Modal)，可完整展示对应版本的更新说明
- 新增「30 天内不再提醒」snooze 选项，已暂缓的用户在 30 天内不再弹出更新提示
- 统一 Vite 与 Tauri devUrl 端口为 5173，本地调试不再需要手动改端口

## v0.2.5 — 2026-05-22

### 修复

- 修复自动更新功能：添加 `createUpdaterArtifacts` 配置，使构建时生成签名文件和 latest.json

## v0.2.4 — 2026-05-21

### 改进

- ICC 配置文件切换支持多显示器（按选定显示器应用）
- 数字震动（Digital Vibrance）调节支持多显示器
- 切换显示器时自动读取对应显示器的驱动设置

### 修复

- 修复 ICC 滤镜始终应用到主显示器的问题
- 修复数字震动调节始终作用于主显示器的问题

## v0.2.3 — 2026-05-19

### 改进

- 新增文档站"为什么选择 Filter Manage"页面，阐述软件设计理念
- 更新 README.md，增加产品定位说明

## v0.2.2 — 2026-05-19

### 新功能

- 新增版本号展示（标题栏显示当前版本）
- 新增自动更新功能（启动时检查 GitHub Releases 新版本）

## v0.2.1 — 2026-05-17

### 改进

- 优化前端整体界面样式展示
- 改进颜色调节器、配置管理、预览图片等组件样式
- 优化深色模式下的视觉效果

## v0.2.0 — 2026-05-17

### 修复
- 修复数字振动（Digital Vibrance）设置不生效的问题
